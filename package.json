{
  "name": "yojanasetu",
  "private": true,
  "scripts": {
    "dev": "bash ./scripts/dev.sh",
    "dev:clean": "bash ./scripts/dev.sh --clean",
    "dev:backend": "bash -lc 'cd backend && if [ ! -x .venv311/bin/python ]; then uv python install 3.11 && uv venv --python 3.11 .venv311 && uv pip install --python .venv311/bin/python -r requirements.txt; fi && .venv311/bin/python -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload'",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
